name: retrofit_wrapper
description: retrofit_wrapper annotation classes
version: 0.0.1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  meta: ^1.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
